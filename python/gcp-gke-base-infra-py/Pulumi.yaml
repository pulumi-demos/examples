name: gcp-gke-base-infra-py
runtime:
  name: python
  options:
    virtualenv: venv
description: A Google Kubernetes Engine (GKE) cluster, with canary deployment
config:
  gcp:project: pulumi-ce-team
  gcp:zone: us-central1-a
