[project]
name = "snowpipe-data-loading"
version = "0.1.0"
description = "Production-grade data loading with Pulumi, AWS S3, and Snowpipe"
requires-python = ">=3.11"
dependencies = [
    "pulumi>=3.0.0,<4.0.0",
    "pulumi-aws>=7.0.0",
    "pulumi-snowflake>=2.0.0",
    "pulumi-github>=6.0.0",
    "pulumi-random>=4.0.0",
    "pulumi-tls>=5.0.0",
]
